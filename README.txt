This is simple binary classification model.

The keras model is saved in folder 'model'

The webapp.py has the flask web application

The model.ipynb has the python notebook of the model

The images folder has the images used for training

Please try again if web application show Error Occured!
